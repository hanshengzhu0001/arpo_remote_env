# Core ML/AI packages
torch==2.9.1
transformers==4.57.6
accelerate==1.12.0
qwen-vl-utils==0.0.11

# OSWorld dependencies
numpy==1.26.4
Pillow==11.2.1
opencv-python==4.8.1.78
pyautogui==0.9.54
pynput==1.7.6
gymnasium==0.28.1
psutil==6.1.0
tqdm==4.65.0
pandas==2.2.3
matplotlib==3.7.4

# API clients
openai==2.15.0
requests==2.32.5
requests-toolbelt==1.0.0
backoff

# Web server (for inference servers)
flask==3.0.0

# Remote env server (Mac/CPU) and verl trainer imports
ray==2.54.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Utilities
PyYAML==6.0.2
lxml
cssselect
xmltodict
beautifulsoup4
tiktoken
loguru

# Document processing (for OSWorld tasks)
openpyxl
python-docx
python-pptx
pypdf
pymupdf
pdfplumber
odfpy

# Media processing
ImageHash
scikit-image
librosa
mutagen
pyacoustid
pygame

# Other OSWorld dependencies
PyGetWindow
rapidfuzz
playwright==1.58.0
func-timeout
formulas
fastdtw
easyocr
gdown
wrapt_timeout_decorator
fabric

# Monitoring
wandb==0.25.0

# verl trainer (main.py, ray_trainer, fsdp_workers, reward, rollout)
omegaconf==2.3.0
tensordict==0.11.0
tensorboard==2.20.0
vllm==0.16.0
datasets==4.6.0
codetiming==1.4.0
torchdata==0.11.0
mathruler==0.1.0
pylatexenc==2.10
# desktop_env (OSWorld) optional deps when using local env
pydrive
chardet
borb
pypdf2
boto3
azure-identity
azure-mgmt-compute
azure-mgmt-network

# Note: Skip GPU-only packages for Mac CPU compatibility:
# - flash_attn (GPU only)
# - liger_kernel (GPU only)
# - vllm (GPU only)
