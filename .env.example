# Remote env server (AWS provider). Copy to .env and fill in, or run start_remote_env_aws.sh on EC2 to auto-detect.
# Optional: DEFAULT_TTL_MINUTES, ENABLE_TTL, AWS_SCHEDULER_ROLE_ARN (see OSWorld/desktop_env/providers/aws/config.py)
# Optional: OPENAI_API_KEY (for agents/evaluation that call OpenAI)

AWS_REGION=us-east-1
AWS_SUBNET_ID=subnet-xxxxxxxxxxxxxxxxx
AWS_SECURITY_GROUP_ID=sg-xxxxxxxxxxxxxxxxx
# OPENAI_API_KEY=sk-...
